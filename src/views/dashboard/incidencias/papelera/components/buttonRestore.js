import { Button } from '@mui/material'

export const RestoreButton = ({ onClick, disabled }) => {
  <Button variant="contained" color="primary" onClick={onClick} disabled={disabled}>
    Restaurar
  </Button>
}